# Bibliotecas

A versão GitHub utiliza bibliotecas web pelos endereços CDN registrados em `index.html`. O núcleo local de dados não depende de arquivos JavaScript externos em `dados/`, pois está incorporado no HTML.
